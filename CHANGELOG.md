# 更新日志 · Changelog

本项目版本号遵循 `主.次.修订`。日期以功能整理为准。

## v1.13.0 — UI 全面升级：设计令牌、自定义壁纸与毛玻璃

- 主题系统重建为设计令牌体系：深浅两套主题由同一模板生成，统一圆角/字号/间距刻度（数据大数字统一 28px），消除深浅主题四处漂移；对外 API 与配置键不变，旧测试零改动通过。
- 控件全主题化：右键菜单、下拉弹层、复选框/单选钮（含列表内勾选）、工具按钮、进度条、列表项、ToolTip、对话框、微调框箭头、滚动条等此前呈 Windows 原生样式的控件全部跟随主题，深色主题不再有原生残留。
- 新增「自定义壁纸 + 可调毛玻璃」：顶栏「🎨 外观」可设置背景图片、毛玻璃强度（模糊半径 0–32px）与面板不透明度（55–100%），实时预览、确定持久化、取消自动回滚；壁纸自动复制到应用数据目录防止失效；默认关闭，观感与纯色主题完全一致（壁纸预模糊按图缓存，拖动防抖）。
- 内联样式清零：11 处绕过主题的硬编码样式收编为共享 ValueCard 与 theme.state_color()；主操作/危险操作按钮语义全面铺开（primary/danger）。
- 图表随主题：8 个绘图页曲线色板按主题切换（每主题 8 色），示波器 A/B 光标与悬停探针浮层同步换肤，动态新建曲线不再回退默认色。
- 统一 22 页边距/间距节奏；修复顶栏「弯道」快捷跳转指向错误页、启动主题回退方向不一致、对话框按钮未汉化三处小问题。
- 新增 16 项主题/外观测试（合计 135 项回归通过）；两主题 × 毛玻璃开关共 53 张全页面截图完成视觉验收。
- 版本号提升至 v1.13.0。

## v1.12.0 — 调参工作流与赛道工具扩展

- AI 调参新增「自动扫参」：逐档下发候选参数并发阶跃实测，按超调/上升时间/整定时间/稳态误差自动评分排序，结束后自动恢复原参数并可一键应用最优值；结果写入实验档案。
- 赛道工程新增「轨迹重建」：由车速 + 横摆角速度航位推算绘制单圈轨迹并按圈叠加对比，比例尺（m/单位）可校准，断流不产生位置跳变。
- 新增「复盘回放」页：遥测会话一键录制为 JSONL（20 万帧自动停止），0.5x~4x 速度回放到整条遥测总线，示波器/弯道分析/诊断全部可对历史数据复盘。
- 示波器新增上位机派生数学通道：@err_sq（误差平方）、@power_w（电池功率，自动汇总全部电流通道），并新增「派生」工作组，录制/导出/探针全兼容。
- 新增「下线检查」页：通信频率、电池电压、IMU 静止零偏、固件版本四项自动检查 + 三项人工确认，一键生成独立 HTML 交付报告。
- 新增「赛道分享」页：把遥测总线以只读 SSE 服务共享给局域网手机浏览器（零新增依赖，无任何控制入口，急停仍只在上位机）。
- 新增「仿真闯关」页：三个调参练习关卡（驯服超调/提速响应/航向收敛），自动注入初始参数、发阶跃、按指标判分记星，星级本地保存（仅限仿真连接）。
- 固件版本协议：参考固件 dctp_port v1.1.0 新增 CMD fw_version 上报版本号（NOTE 事件），总览页新增「车上固件版本」卡片，连接后自动探测。
- 参数方案页支持「导出调参卡片」：参数 + 固件版本历史打包为 YAML，与「加载并下发」完全兼容，方便车队分享。
- 新增首次运行使用引导（非阻塞，四步指路）与顶栏「引导」按钮。
- CAN 基座：SLCAN 帧编解码（core/slcan.py，含测试）与《CAN 接入设计》文档；传输层待 MCU 桥接固件就绪后再接入。
- 版本号提升至 v1.12.0。

## v1.11.0 — 烧录链路打磨与固件版本库

- 固件烧录新增「固件版本库」：每次烧录自动按 SHA256 快照存档（同镜像去重），可写备注、按结果归档，支持从历史版本一键回退重烧；失败的烧录记录同样是恢复点。
- MSPM0 BSL 驱动：擦除改为整段用户区（杜绝短固件尾部残留旧代码），连接/擦除/校验各阶段均可即时取消（此前最长等待 15 秒），完整打印 BSL 身份信息；烧录状态机新增「已取消」终态，取消不再被误报为失败。
- STM32（stm32flash）烧录：显式传 `-m 8e1`（AN3155 偶校验，不再依赖用户猜蓝牙模块配置），写入进度由可视化进度条实时显示、日志区只保留阶段与错误信息，并捕获此前被丢弃的 stderr 错误详情。
- 新增各芯片族固件尺寸上限（STM32F1 ≤1MB / STM32F4 ≤2MB / MSPM0G3507 ≤128KB），重复的 128KB 检查收敛到一处。
- 通信可靠性：串口发送失败不再被静默吞掉（系统诊断新增「TX失败」计数与建议）；带 seq 但不匹配的旧 ACK 直接丢弃，避免同 key 参数串扰；参数方案保存前对未回读过的参数弹窗确认，防止存出半截方案。
- 配置与体验：主界面与烧录页支持本机串口扫描下拉；烧录端口/波特率/串口格式、车型选择、BLE 上次连接信息全部持久化；电源监控启用车型 YAML 校准参数实现 ADC raw→电压换算；MSPM0 车型注明「9600 仅约束 BSL 烧录链路，调参链路可另配高波特率」。
- 工程卫生：统一 logging（写入 logs/dicar_lab.log，级别由 DICAR_LOG_LEVEL 控制）；修正 v1.7 时代遗留的「烧录按钮禁用」过时说明；四份内容相同的启动 bat 收敛为一份并更新横幅；CAR_LAB/VERSION.txt 同步。
- 版本号提升至 v1.11.0。

## v1.10.0 — 无线烧录拓展 TI MSPM0G3507

- 固件烧录页新增 MSPM0G3507 系列：内置 TI ROM BSL 驱动（SLAU887 协议，固定 9600-8N1、UART0 PA10/PA11），选择该系列时波特率自动锁定、固件超 128KB 在访问串口前即被拒绝，烧录在独立线程执行可中途取消。
- 新驱动由迁移前的 Rust BSL 实现（commit 56aa975）移植，并修正其两处遗留问题：改用绝对闪存地址（MSPM0G 基址 0x41C00000，原实现从 0 起擦写会被设备拒绝），并文档化 CRC-32 终值异或差异（实板握手失败时的首查项）。
- 通过 BSL 设备模拟器完成 12 项全流程协议测试：正常烧录、密码错误、传输/响应错误码、响应 CRC 损坏、写入位翻转校验、中途取消、8 字节对齐补齐、断线等。**MSPM0 支持尚未在实车验证。**
- MSPM0 车型档案补充 `flash.family` 标记并将链路波特率调整为 9600-8N1（与 ROM BSL 及蓝牙模块配置一致）；STM32（F1/F4）的 stm32flash 烧录路径保持不变。
- 版本号提升至 v1.10.0。

## v1.9.0 — 无线烧录拓展 STM32F4

- 固件烧录页新增「芯片系列」选择（STM32F1 / STM32F4），默认跟随当前车型档案的 `flash.family` 标记，切换系列同步更换 bootloader 进入步骤与排障提示。
- 新增车型档案 `stm32f4_dual_motor.yaml`（STM32F4 · 双电机智能车），并给 STM32F103 档案补充 `flash.family` 标记。
- F4 与 F1 共用同一 AN3155 串口 bootloader 协议，stm32flash 命令不变；F4 指引明确 bootloader 以 USART1（PA9/PA10）为主接口（部分型号支持 USART3 等，以 AN2606 为准）、扇区擦除耗时更长、个别型号高波特率握手受限时可降 9600。**F4 目前为协议层面支持，尚未在实车端到端实测。**
- 版本号提升至 v1.9.0。

## v1.8.2 — 版权归属与仓库更名

- 项目版权变更为 tluda. 所有（MIT），依据 MIT 条款在 `THIRD-PARTY-NOTICES.md` 中保留 IKUN CAR LAB Contributors 的原始声明，并登记内置 stm32flash（GPL-2.0）组件。
- 仓库由 `zhaoxc857/DiCar_Tune` 更名为 `zhaoxc857/DiCar-LAB`，README、小白用户指南、贡献指南中的下载与 Issue 链接同步更新。
- CI 发布流程改用规范 API 端点（修复仓库改名后发布脚本的 301/401 问题）；新增单文件版构建脚本 `build_onefile_windows.ps1` 与对应发布资产。
- 版本号提升至 v1.8.2，发布资产携带新 LICENSE。

## v1.8.1 — 单文件版桌面 App

- 新增单文件发行形态 `DiCAR-LAB-v1.8.1-Windows-x64-onefile.exe`：一个独立 exe 双击即用，无需解压文件夹（PyInstaller onefile，启动时自解压约 2~3 秒）；新增 `dicar_lab_onefile.spec` 与 `build_onefile_windows.ps1`。
- 重构资源/数据路径（`core/paths.py`）：打包版用户数据（参数方案、实验历史、波形导出、自定义环、日志）统一写入 `%LOCALAPPDATA%\DiCAR LAB\`，不再写入程序目录，升级或移动程序不丢数据；只读资源（车型、文档、内置工具）从打包目录读取，启动检查中显示数据目录路径。
- CI 同步构建单文件版，Release 资产附带各自 `SHA256SUMS` 校验文件。
- 版本号提升至 v1.8.1。

## v1.8.0 — 无线烧录与示波器录制回看

- 固件烧录页接入真实后端：自动探测内置 `stm32flash.exe`，通过 HC-05 蓝牙串口无线烧录 STM32 固件（自动断开车辆连接、实时日志、按 FlashJob 状态机校验与失败诊断）。连续烧录模式暂未开放。
- 修复主窗口卡死：串口打开与读写移入独立工作线程，蓝牙串口连接不再阻塞界面；断开连接立即释放串口（含回归测试）。
- 新增串口固件配置模式：车载 USER 键上电触发，自动将 HC-05 配置为 115200-8E1 并回读验证（OLED 显示 OK/NO），与 STM32 出厂串口 bootloader 协议匹配。
- STM32F103 巡线车链路全面提速至 115200 波特率（固件、HC-05、车型配置同步）。
- 示波器新增录制 / 导出 CSV / 加载回看：全量遥测录制（上限 6 万样本）、`reports/waves/` 时间戳文件、回看模式支持 A/B 游标与数据探针（`core/wave_store.py`）。
- 系统诊断断开后显示「—」而非残留频率值。
- 版本号统一提升至 v1.8.0。

## v1.7.0 — Windows 桌面版与安全烧录边界

- 发布免安装的 Windows x64 便携包 `DiCAR-LAB-v1.7.0-Windows-x64.zip`，解压后直接运行 `DiCAR LAB.exe`，无需预装 Python。
- 增加 PyInstaller 一目录构建、离屏启动冒烟测试、SHA-256 校验文件和 GitHub Actions 自动 Release。
- 重新设计按钮交互：普通、主操作、危险操作在深浅主题下均具备清晰的按下、键盘焦点与禁用状态，按下时保持外框尺寸稳定。
- 新增固件烧录工作区和受约束的任务状态机，为后续可扩展后端与无限烧录预留安全边界。
- 当前烧录运行按钮保持禁用，没有接入外部烧录命令；真实烧录能力仍属于后续路线图。
- 统一版本显示、冻结环境资源定位、桌面下载说明和 STM32F103 + HC-05 接入指引。

## v1.6.1 — 麦轮车型、专家模式与车型插件化
- **恢复 / 扩充车型库**：新增麦克纳姆轮全向车、两轮平衡车、舵机循迹车、自定义车型模板。
- **麦轮专用支持**：
  - 电机层 FL/FR/RL/RR（`fl_rpm`/`fr_rpm`/`rl_rpm`/`rr_rpm` + PWM / 电流 / 编码器）。
  - 底盘运动解算层 Vx/Vy/Wz（目标 + 实际），新增「麦轮运动」调试页，可直接发送底盘目标并在线调三轴 PID。
  - 示波器「麦轮运动」专属工作组（Vx/Vy/Wz 目标与实际 + 四轮 RPM/PWM + 电流），一键选通道。
  - 仿真器加入麦轮运动学（`cmd_vx`/`cmd_vy`/`cmd_wz` → 逆解四轮），不接硬件即可验证。
- **专家模式正式化**：自定义环专家模式新增「通信」组——对任意 MCU 字段直接 GET / SET、查看 ACK、发送 RAW JSON。
- **车型插件化**：车型加载同时支持扁平 `vehicles/<name>.yaml` 与插件目录 `vehicles/<name>/config.yaml`，新增车型只需加一个文件夹，避免「加功能导致车型消失」；新增 `CONTRIBUTING.md` 车型贡献指南。
- **示波器性能**：曲线改为持久对象增量更新（不再每帧重建），多通道下 CPU 明显下降。
- **图表统一中文化**：Speed / Heading Lab、电源监控的曲线名与坐标轴改中文，并跟随黑/白主题（移除硬编码深色背景）。
- **健壮性**：读取本地自定义环 / 下发参数方案失败不再静默吞异常，改为写 stderr 日志并提示。
- 版本号统一提升至 v1.6.1。

## v1.6.0 — 首个开源版本
- 统一版本号（此前 README/入口/窗口标题分别写着 v1.2 / v1.3 / v1.5.1，已全部对齐为 v1.6.0）。
- 整理仓库以适配开源：新增 `.gitignore`、`.gitattributes`、`LICENSE`(MIT)、根 `README.md` 与本 `CHANGELOG.md`；修复 `docs/` 中被损坏的中文文件名；虚拟环境、缓存、日志、运行时数据不再入库。
- 保留 Launcher 一键启动、PID / AI 调参、弯道分析等既有能力。

## v1.5.x — 专业调车优化
- 示波器中文曲线名 + 协议 key 辅助显示；速度/航向/角速度/电源/电机/循迹工作组。
- 自定义环真正区分普通 / 专家模式。
- 启动环境自检与 `logs/error.log`；新增 `tools/static_startup_check.py` 扫描 Qt 控件漏导入。
- 修复：自定义环缺少 `QPlainTextEdit` 导入、自检时机、顶部「弯道」快捷入口索引等。

## v1.4.x — 调车体验升级
- 示波器工作组一键选通道、局部 Y 轴自动范围；保留探针、A/B 游标、固定提示。
- Speed Lab 单独放大 Error 曲线并实时显示参数发送状态。
- 顶部「速度/航向/示波器/弯道」快速入口。
- 修复：pyqtgraph 别名被局部变量覆盖、QComboBox 误建为 tuple 等启动问题。

## v1.3.x — Corner Analyzer 与可靠参数同步
- 赛道工程新增自动入弯/出弯统计、左右弯识别、每弯速度与耗时、最大循迹误差与峰值角速度（滞回阈值 + 持续时间确认）。
- 默认主题改为白色。
- 可靠参数同步：key 冲突检查、发送缓冲区、同 key 合并、`seq` 序号、ACK `ok/error`、超时重试、回读校验、断线保留待发送。
- 文档：`docs/MCU_to_PC_通信协议与可靠参数同步开发手册_v1.3.1.md`。

## v1.2 — UI 与示波器重构
- 左侧分组导航替代顶部大量 Tab。
- 示波器三栏布局、通道搜索、预设、实时数值、固定数据探针、A/B 双游标。
- 车型模板扩充到 10 个；切换车型自动重载字段映射与连接默认值。

## v1.0 / v1.1 — 基础能力
- 总览、示波器、Speed/Heading Lab、自定义 PID 环、AI 调参、电源 ADC、单电机实验、底盘调试、参数读写与方案、赛道工程、实验档案、系统诊断、协议监视器、MSP 适配。
- 连接方式：串口 / 蓝牙串口 / BLE / TCP / 仿真。
