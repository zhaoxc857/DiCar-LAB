{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "DiCar main window core invoke and Channel access only",
  "windows": ["main"],
  "permissions": ["core:default"]
}
