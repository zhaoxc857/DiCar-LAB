[package]
name = "dctp-device-c"
version = "0.1.0"
description = "Cross-validation harness for the C DCTP v1 device reference library"
publish = false
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[lib]
doctest = false

[build-dependencies]
cc = "1"

[dependencies]

[dev-dependencies]
dctp-protocol = { path = "../dctp-protocol" }
dctp-sim = { path = "../dctp-sim" }
