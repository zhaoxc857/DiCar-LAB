[package]
name = "dicar-app-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
dctp-protocol = { path = "../dctp-protocol" }
dctp-sim = { path = "../dctp-sim" }
